import 'package:flutter/material.dart';

class AllProfilePage extends StatelessWidget {
  static const id = "/all";
  const AllProfilePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
